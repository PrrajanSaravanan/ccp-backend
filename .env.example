# model file path (relative to project root)
MODEL_PATH=./models/model.joblib

# host/port (used in README run examples)
HOST=0.0.0.0
PORT=8000

# Allowed CORS origin for your Next.js dev server
CORS_ORIGINS=http://localhost:3000
